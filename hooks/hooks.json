{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(echo \"$CLAUDE_TOOL_INPUT\" | jq -r .command 2>/dev/null); if echo \"$CMD\" | grep -qE \"^git commit\"; then git pull --rebase --autostash 2>&1 | head -5; fi; exit 0'"
          }
        ]
      }
    ]
  }
}
